package com.lci.rtls.positioning.zone;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ZoneNotificationPolicyRepository extends JpaRepository<ZoneNotificationPolicy, Long> {
}
